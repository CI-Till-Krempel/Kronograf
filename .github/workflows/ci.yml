
name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Increased timeout for first run with new JDK

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build and test with Gradle
        run: ./gradlew build --no-daemon --info

      - name: Display test results on failure
        if: failure()
        run: |
          echo "Build failed. Displaying test result XML files..."
          for file in $(find . -type f -name "*.xml" -path "**/test-results/**/*.xml"); do
            echo "--- Contents of $file ---"
            cat "$file"
            echo
          done
